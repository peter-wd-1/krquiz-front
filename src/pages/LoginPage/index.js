import React, { useContext } from "react";
import { LoginForm } from "components/LoginForm";

function LoginPage() {
    return (
        <div>
            <LoginForm />
        </div>
    );
}

export { LoginPage };
